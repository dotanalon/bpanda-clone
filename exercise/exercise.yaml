---
- name: Common tasks FTW
  hosts: all
  become: yes
  pre_tasks:
    - name: Set hostname
      hostname: name={{inventory_hostname}}

- name: Base bamboo service
  hosts: applicationservers
  become: yes
  roles:
    - { role: nodejs, tags: nodejs }
    - { role: exercise, tags: exercise }
